hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 20000

spring:
  config:
    import: "optional:configserver:"

rates:
  url: https://api.exchangeratesapi.io